let init = (state) =>
  /* state |> RenderJobSystem.execRenderInitJobs([@bs] DeviceManagerService.unsafeGetGl(state.deviceManagerRecord)); */
  state;

let render = (state) =>
  /* state |> RenderJobSystem.execRenderRenderJobs([@bs] DeviceManagerService.unsafeGetGl(state.deviceManagerRecord)); */
  state;