let setTransformCount = count => POConfigCPRepo.setTransformCount(count);

let setGeometryCount = count => POConfigCPRepo.setGeometryCount(count);

let setGeometryPointCount = count =>
  POConfigCPRepo.setGeometryPointCount(count);

let setDirectionLightCount = count =>
  POConfigCPRepo.setDirectionLightCount(count);

let setPBRMaterialCount = count => POConfigCPRepo.setPBRMaterialCount(count);
