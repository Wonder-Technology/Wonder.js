

import * as InitSkyboxJobUtils$Wonderjs from "../../utils/InitSkyboxJobUtils.js";

function execJob(flags, state) {
  return InitSkyboxJobUtils$Wonderjs.exec(state);
}

export {
  execJob ,
  
}
/* InitSkyboxJobUtils-Wonderjs Not a pure module */
