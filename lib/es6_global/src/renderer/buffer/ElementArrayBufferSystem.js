// Generated by BUCKLESCRIPT VERSION 2.1.0, PLEASE EDIT WITH CARE
'use strict';

import * as VboBufferSystem$Wonderjs     from "./VboBufferSystem.js";
import * as VboBufferPoolCommon$Wonderjs from "./VboBufferPoolCommon.js";

function createBuffer(gl, data, state) {
  var buffer = VboBufferPoolCommon$Wonderjs.getElementArrayBuffer(gl, state);
  gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER, buffer);
  gl.bufferData(gl.ELEMENT_ARRAY_BUFFER, data, gl.STATIC_DRAW);
  gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER, null);
  return buffer;
}

function getOrCreateBuffer(gl, geometryIndex, bufferMap, getDataFunc, state) {
  return VboBufferSystem$Wonderjs.getOrCreateBuffer(gl, /* tuple */[
              geometryIndex,
              bufferMap
            ], /* tuple */[
              createBuffer,
              getDataFunc
            ], state);
}

export {
  createBuffer      ,
  getOrCreateBuffer ,
  
}
/* VboBufferSystem-Wonderjs Not a pure module */
