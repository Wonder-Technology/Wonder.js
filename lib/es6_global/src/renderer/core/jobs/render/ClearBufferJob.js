// Generated by BUCKLESCRIPT VERSION 2.1.0, PLEASE EDIT WITH CARE
'use strict';

import * as Js_option                    from "../../../../../../../node_modules/bs-platform/lib/es6/js_option.js";
import * as RenderConfigSystem$Wonderjs  from "../../../config/render/RenderConfigSystem.js";
import * as DeviceManagerSystem$Wonderjs from "../../../device/DeviceManagerSystem.js";

function _getBitFromFlags(gl, flags) {
  var bit = /* None */0;
  if (flags.includes("COLOR_BUFFER")) {
    var match = bit;
    bit = match ? /* Some */[match[0] | gl.COLOR_BUFFER_BIT] : /* Some */[gl.COLOR_BUFFER_BIT];
  }
  if (flags.includes("DEPTH_BUFFER")) {
    var match$1 = bit;
    bit = match$1 ? /* Some */[match$1[0] | gl.DEPTH_BUFFER_BIT] : /* Some */[gl.DEPTH_BUFFER_BIT];
  }
  if (flags.includes("STENCIL_BUFFER")) {
    var match$2 = bit;
    bit = match$2 ? /* Some */[match$2[0] | gl.STENCIL_BUFFER_BIT] : /* Some */[gl.STENCIL_BUFFER_BIT];
  }
  return Js_option.getExn(bit);
}

function getJob(param, gl, state) {
  var flags = param[0];
  if (flags) {
    return DeviceManagerSystem$Wonderjs.clearBuffer(gl, _getBitFromFlags(gl, flags[0]), state);
  } else {
    return RenderConfigSystem$Wonderjs.throwJobFlagsShouldBeDefined(/* () */0);
  }
}

export {
  _getBitFromFlags ,
  getJob           ,
  
}
/* RenderConfigSystem-Wonderjs Not a pure module */
