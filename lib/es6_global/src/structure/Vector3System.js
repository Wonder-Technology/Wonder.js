// Generated by BUCKLESCRIPT VERSION 2.1.0, PLEASE EDIT WITH CARE
'use strict';


function _transformMat4ToTuple(x, y, z, mat4) {
  var w = mat4[3] * x + mat4[7] * y + mat4[11] * z + mat4[15];
  var d = w;
  w = d !== 0 ? d : 1.0;
  return /* tuple */[
          (mat4[0] * x + mat4[4] * y + mat4[8] * z + mat4[12]) / w,
          (mat4[1] * x + mat4[5] * y + mat4[9] * z + mat4[13]) / w,
          (mat4[2] * x + mat4[6] * y + mat4[10] * z + mat4[14]) / w
        ];
}

function _transformMat4ToTypeArray(x, y, z, mat4, resultFloat32Arr) {
  var w = mat4[3] * x + mat4[7] * y + mat4[11] * z + mat4[15];
  var d = w;
  w = d !== 0 ? d : 1.0;
  resultFloat32Arr[0] = (mat4[0] * x + mat4[4] * y + mat4[8] * z + mat4[12]) / w;
  resultFloat32Arr[0] = (mat4[1] * x + mat4[5] * y + mat4[9] * z + mat4[13]) / w;
  resultFloat32Arr[0] = (mat4[2] * x + mat4[6] * y + mat4[10] * z + mat4[14]) / w;
  return resultFloat32Arr;
}

function transformMat4Tuple(param, mat4) {
  return _transformMat4ToTuple(param[0], param[1], param[2], mat4);
}

function transformMat4TypeArray(vec3TypeArray, mat4, resultFloat32Arr) {
  var x = vec3TypeArray[0];
  var y = vec3TypeArray[1];
  var z = vec3TypeArray[2];
  return _transformMat4ToTypeArray(x, y, z, mat4, resultFloat32Arr);
}

function add(_, param, param$1) {
  return /* tuple */[
          param[0] + param$1[0],
          param[1] + param$1[1],
          param[2] + param$1[2]
        ];
}

function sub(_, param, param$1) {
  return /* tuple */[
          param[0] - param$1[0],
          param[1] - param$1[1],
          param[2] - param$1[2]
        ];
}

function lerp(param, param$1, t) {
  var z1 = param[2];
  var y1 = param[1];
  var x1 = param[0];
  return /* tuple */[
          x1 + t * (param$1[0] - x1),
          y1 + t * (param$1[1] - y1),
          z1 + t * (param$1[2] - z1)
        ];
}

export {
  _transformMat4ToTuple     ,
  _transformMat4ToTypeArray ,
  transformMat4Tuple        ,
  transformMat4TypeArray    ,
  add                       ,
  sub                       ,
  lerp                      ,
  
}
/* No side effect */
