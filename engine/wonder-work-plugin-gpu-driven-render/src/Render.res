let buildRenderPassState = (): IWebGPUForJs.PassEncoder.Render.descriptor => {
  //TODO implement
  Obj.magic(1)
}

let execBatches = (
  batches: array<Update.batch>,
  renderPassState: IWebGPUForJs.PassEncoder.Render.descriptor,
): unit => {
  //     const passEncoder = commandEncoder.beginRenderPass({
  //         renderPassState
  //     })

  //     let bundleArr = []

  // batches ->WonderCommonlib.ArraySt.reduceOneParam(
  // (. bundleArr, batch) =>{
  //         return bundleArr.concat([batch.renderBundle -> WonderCommonlib.OptionSt.getExn]);
  // }, [])

  //     passEncoder.executeBundles(bundleArr);

  //     passEncoder.endPass();

  //TODO implement
  Obj.magic(1)
}
