language: node_js

addons:
  chrome: stable

cache:
  yarn: true
  directories:
    - node_modules

node_js:
  - "8.7"

install:
  - yarn install
script:
  - gulp prepareForCI
  - gulp build
  - npm run test:pf
  - npm run test:coverage
after_success:
  - npm run codecov