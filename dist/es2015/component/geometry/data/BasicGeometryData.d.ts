import { GeometryData } from "./GeometryData";
import { Geometry } from "../Geometry";
export declare class BasicGeometryData extends GeometryData {
    static create(geometry: Geometry): BasicGeometryData;
}
