import { CameraController } from "../CameraController";
import { Camera } from "../../Camera";
export declare class BasicCameraController extends CameraController {
    static create(cameraComponent: Camera): BasicCameraController;
}
