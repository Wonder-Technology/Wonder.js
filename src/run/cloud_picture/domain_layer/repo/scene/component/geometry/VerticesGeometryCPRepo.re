let getVertices = index => {
  GeometryCPRepoDp.getVertices(index);
};

let setVertices = (index, data) => {
  GeometryCPRepoDp.setVertices(index, data);
};

let hasVertices = index => {
  GeometryCPRepoDp.hasVertices(index);
};
