let getNormals = index => {
  GeometryCPRepoDp.getNormals(index);
};

let setNormals = (index, data) => {
  GeometryCPRepoDp.setNormals(index, data);
};

let hasNormals = index => {
  GeometryCPRepoDp.hasNormals(index);
};
