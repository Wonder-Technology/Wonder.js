let getTexCoords = index => {
  GeometryCPRepoDp.getTexCoords(index);
};

let setTexCoords = (index, data) => {
  GeometryCPRepoDp.setTexCoords(index, data);
};

let hasTexCoords = index => {
  GeometryCPRepoDp.hasTexCoords(index);
};
