let getIndices = index => {
  GeometryCPRepoDp.getIndices(index);
};

let setIndices = (index, data) => {
  GeometryCPRepoDp.setIndices(index, data);
};

let hasIndices = index => {
  GeometryCPRepoDp.hasIndices(index);
};

let getIndicesCount = index => {
  GeometryCPRepoDp.getIndicesCount(index);
};
