let getTransformCount = () => {
  CPRepo.getPOConfig().transformCount;
};

let getPBRMaterialCount = () => {
  CPRepo.getPOConfig().pbrMaterialCount;
};

let getGeometryPointCount = () => {
  CPRepo.getPOConfig().geometryPointCount;
};

let getGeometryCount = () => {
  CPRepo.getPOConfig().geometryCount;
};

let getDirectionLightCount = () => {
  CPRepo.getPOConfig().directionLightCount;
};
