@body
// todo u_opacity
    // gl_FragColor = vec4(totalColor.rgb, totalColor.a * u_opacity);
    // gl_FragColor = vec4(totalColor.rgb, totalColor.a * 1.0);
    gl_FragColor = vec4(1.0,0.5,1.0, 1.0);
@end
