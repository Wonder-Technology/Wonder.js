export var init = (gl:any, GBufferData:any) => {
    create gbuffer shader, program
    create light pass shader, program

    add gbuffer textures to gbuffer data?
        ???

    init gbuffer


}

export var draw = () => {

}
