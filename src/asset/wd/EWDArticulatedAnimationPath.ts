module wd{
    export enum EWDKeyFrameAnimationPath{
        TRANSLATION = <any>"translation",
        ROTATION = <any>"rotation",
        SCALE = <any>"scale"

        //todo support more path(e.g. TEXTURE_OFFSET)
    }
}
