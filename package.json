{
  "name": "wonder.js",
  "version": "1.0.0-alpha.10",
  "authors": "DreamForest",
  "description": "3d webgl engine",
  "homepage": "https://github.com/DreamForeast/Wonder.js",
  "license": "MIT",
  "main": "",
  "scripts": {
    "start": "node ./server/app.js",
    "codecov": "cat test/coverage/lcov.info | codecov",
    "watch": "bsb -clean-world -make-world -w",
    "build": "bsb -clean-world -make-world",
    "test": "npm run build && jest --config jest.json",
    "test:coverage": "npm run build && jest --config jest_coverage.json",
    "test:watch": "npm run build && jest --watch --config jest.json"
  },
  "devDependencies": {
    "babel": "^6.23.0",
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "bs-platform": "2.0.0",
    "codecov.io": "^0.1.6",
    "express": "^4.16.1",
    "gulp": "^3.9.1",
    "gulp-sync": "^0.1.4",
    "http": "^0.0.0",
    "wonder-bs-jest": "latest",
    "wonder-bs-sinon": "^0.1.3",
    "wonder-glsl-compiler": "latest"
  },
  "dependencies": {
    "bs-json": "^0.2.3",
    "wonder-commonlib": "^0.2.11"
  }
}
